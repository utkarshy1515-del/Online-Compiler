{
  "name": "coderunner-backend",
  "version": "1.0.0",
  "description": "Backend API for CodeRunner online compiler",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "docker:build": "cd docker && chmod +x build-images.sh && ./build-images.sh"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dockerode": "^4.0.2",
    "express": "^4.18.2",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
